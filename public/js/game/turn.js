// *** TURN ***

var Turn = {
    number:null,
    shortName:null,
    init:function(){
        for(i in turnHistory){

        }

        this.number = turnHistory[i].number;
        this.shortName = turnHistory[i].shortName;
    }

}
